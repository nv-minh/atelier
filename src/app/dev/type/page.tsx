// ── /dev/type — Font & glyph probe ───────────────────────────────
// Stub. Content will be filled by Task 5 of Plan 1 (ui-atelier-v2).
export default function DevTypePage() {
  return (
    <main className="shell py-12">
      <h1 className="display text-display-lg mb-4">/dev/type</h1>
      <p className="text-soft text-lg mb-8">
        Font &amp; glyph probe — placeholder. Content arrives in Task 5.
      </p>
      <div className="card-atelier p-6">
        <p className="text-soft">Preview area — empty for now.</p>
      </div>
    </main>
  );
}
